{
  "name": "usnepalhackathon",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"node scripts/wait-then-frontend.mjs\"",
    "dev:local": "concurrently -n api,web -c blue,green \"npm run start --prefix backend\" \"node scripts/wait-then-frontend.mjs\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "dev:backend-only": "npm run start --prefix backend",
    "dev:frontend-only": "VITE_NO_AUTO_API=1 npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
